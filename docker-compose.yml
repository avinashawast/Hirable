version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: hirable-postgres
    environment:
      POSTGRES_USER: hirable_user
      POSTGRES_PASSWORD: hirable_pass
      POSTGRES_DB: hirable
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hirable_user"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
